# .env.example - Template for Environment Variables
# Copy this file to .env and fill in your values
# IMPORTANT: .env is in .gitignore - never commit it!

# Terraform Variables (TF_VAR prefix is required)
export TF_VAR_admin_username="qadmin"
export TF_VAR_admin_password="YourSecurePasswordHere123!"
export TF_VAR_default_subscription_id="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# Azure CLI (if needed)
export AZURE_SUBSCRIPTION_ID="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your values
# 3. Source it: source .env
# 4. Run Terraform: terraform plan